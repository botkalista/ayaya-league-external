

export interface Settings {
    me: { range: boolean },
    nmeChamps: { range: boolean },
    over: { nmeSpells: boolean, performance: boolean },
    missiles: { show: boolean },
    root: { readingTime: number }
}